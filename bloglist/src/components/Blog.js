const Blog = ({ blog }) => {
  return (
    <div>
      <p>Blog title: "{blog.title}"</p>
      <p>Blog author: "{blog.author}"</p>
      *******************************************
    </div>
  );
};

export default Blog;
